version: "2"
sql:
  - engine: "postgresql"
    schema:
      - "internal/db/migrations/"
    queries:
      - "internal/db/queries/static/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_db_tags: true
        sql_package: "pgx/v5"
